/**
 * Selection option constants
 * Contains dropdown/select options for forms and filters:
 * - Salary ranges
 * - Experience levels
 * - Job types
 * - Project types
 * - Work modes
 * - Gender options
 */

/**
 * Salary ranges
 */
export const SALARY_RANGES = [
  "0 - 500",
  "500 - 1,000",
  "1,000 - 2,000",
  "2,000 - 5,000",
  "5,000+",
];

/**
 * Experience levels
 */
export const EXPERIENCE_LEVELS = ["beginner", "intermediate", "expert"];

/**
 * Job types
 */
export const JOB_TYPES = [
  "full-time",
  "part-time",
  "contract",
  "internship",
];

/**
 * Job types for forms (different values than filter job types)
 */
export const JOB_TYPES_FORM = [
  "permanent",
  "contract",
  "freelance",
  "internship",
];

/**
 * Project types
 */
export const PROJECT_TYPES = [
  "short-term",
  "long-term",
  "General",
  "milestone",
];

/**
 * Work modes
 */
export const WORK_MODES = ["remote", "hybrid", "on-site"];

/**
 * Gender options
 */
export const GENDERS = ["male", "female"];

