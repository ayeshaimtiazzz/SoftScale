__pycache__
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info
dist
build
.env
.venv
venv/
ENV/
env/
.pytest_cache
.coverage
htmlcov/
*.log
.DS_Store
embeddings/*.faiss
!embeddings/.gitkeep

__pycache__/
embeddings/

# Exclude zip files (models are already extracted, zip is not needed)
# The merged.zip is 5.5GB and slows down Docker builds significantly
*.zip
**/*.zip

# Note: Model files (.safetensors) are kept in image as fallback
# but are also mounted as volumes in docker-compose.yml for live updates
